{
  "name": "test-calculator-phase1-foundation",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "npm run typecheck",
    "test": "npm run build && node --test test"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
