{
  "name": "scientific-webgl-calculator-phase1",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "npm run typecheck",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "npm run build && node --test tests",
    "validate": "npm run lint && npm run test"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
