{
  "name": "scientific-webgl-calculator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "lint": "tsc --project tsconfig.json --noEmit --pretty false",
    "test": "npm run build --silent && node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
